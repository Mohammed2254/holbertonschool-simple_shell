# Simple Shell

A simple UNIX command interpreter written in C.

## Description
This project is a simple implementation of a UNIX shell that can execute commands
entered by the user.

## Compilation
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 *.c -o hsh

## Usage
./hsh
